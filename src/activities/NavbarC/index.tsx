import { NavbarCContainer } from "@/containers";
import { AppScreen } from "@stackflow/plugin-basic-ui";

const NavbarC = () => {
  return (
    <AppScreen>
      <NavbarCContainer />
    </AppScreen>
  );
};

export default NavbarC;
