import { MainContainer } from "@/containers";
import { AppScreen } from "@stackflow/plugin-basic-ui";

const Main = () => {
  return (
    <AppScreen>
      <MainContainer />
    </AppScreen>
  );
};

export default Main;
