import { NavbarAContainer } from "@/containers";
import { AppScreen } from "@stackflow/plugin-basic-ui";

const NavbarA = () => {
  return (
    <AppScreen>
      <NavbarAContainer />
    </AppScreen>
  );
};

export default NavbarA;
